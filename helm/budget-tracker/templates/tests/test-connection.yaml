apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "budget-tracker.fullname" . }}-test-connection"
  labels:
    {{- include "budget-tracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "budget-tracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
